- name: Launch front container
  community.docker.docker_container:
    name: front
    image: kintz53/tp-devops-front:latest
    state: started
    restart_policy: always
    pull: true
    recreate: true
    docker_host: unix:///var/run/docker.sock
    networks:
      - name: app-network