all:
  vars:
    ansible_user: admin
    ansible_ssh_private_key_file: /home/kenny/id_rsa
    ansible_python_interpreter: /opt/docker_venv/bin/python

    db_name: db
    db_user: usr
    db_password: pwd

  children:
    prod:
      hosts:
        takima:
          ansible_host: kenny.bintz.takima.school
